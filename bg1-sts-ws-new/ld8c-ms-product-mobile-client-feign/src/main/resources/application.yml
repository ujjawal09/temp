spring:
  application:
    name: product-mobile-client
server:
  port: 6062    
eureka:  client:    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone:  http://localhost:8761/eureka/#  instance:
#    hostname: localhost  
    

